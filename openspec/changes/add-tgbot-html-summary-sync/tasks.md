## 1. Implementation
- [ ] 1.1 明确GitHub鉴权与默认仓库/分支配置（含可选作者名/邮箱、默认文件命名规则），以及Telegram命令名称与参数格式。
- [ ] 1.2 实现Telegram命令，解析并校验HTML URL与目标repo输入，可接受可选分支/文件名/作者，向用户确认开始处理。
- [ ] 1.3 实现HTML抓取、主体内容提取与Markdown总结生成，调用兼容OpenAI格式的外部LLM生成不做硬性长度限制的摘要，文首添加YAML frontmatter（标题、来源、创建时间、tags、分类、关键词），正文保留图片与链接。
- [ ] 1.4 实现GitHub写入：可使用用户自定义文件名，否则生成唯一文件名；提交到配置默认分支或请求分支，提交信息引用源URL并可覆盖作者。
- [ ] 1.5 为URL校验、抓取/解析失败、LLM调用失败、GitHub写入失败提供用户可见错误与日志，避免泄露敏感token。
- [ ] 1.6 为总结流水线与GitHub写入路径添加测试（成功与失败用例，包括自定义文件名/作者、frontmatter字段、图片/链接保留）。
